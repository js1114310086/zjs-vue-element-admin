/*
import baseRequest from '../utils/base-request'
import Store from '../store/index'

export function WebRequest(data) {
  // 公共参数
  const keys = {
    token: Store.state.user.token ? Store.state.user.token : '',
    sessionId: Store.state.user
  }
}
*/
